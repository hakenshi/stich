import {PropsWithChildren} from "react"

export function <%= name[0].toUpperCase() + name.slice(1).replace(/[-_ ]+(\w)/g, (_, c) => c ? c.toUpperCase() : '') %>Layout({children}:PropsWithChildren){
    
    return(
        <div>
            {children}
        </div>
    )
}