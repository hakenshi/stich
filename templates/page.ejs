export function <%= name[0].toUpperCase() + name.slice(1).replace(/[-_ ]+(\w)/g, (_, c) => c ? c.toUpperCase() : '') %>Page(){
    
    return(
        <div>
            <%= name %>
        </div>
    )
}